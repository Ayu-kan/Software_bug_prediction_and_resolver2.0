# ==============================================================
# Backend Python Dependencies — Enterprise Bug Risk Platform v2.0
# ==============================================================
# Install with: pip install -r requirements.txt
# Requires: Python 3.10+

# --- API Framework ---
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# --- Security / Auth ---
# (JWT & PBKDF2 implemented via stdlib: hashlib, hmac, base64)
# No extra package needed beyond stdlib

# --- Data Processing & ML ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
xgboost>=1.7.0
joblib>=1.2.0

# --- Static Code Analysis ---
lizard>=1.17.0          # Cyclomatic complexity & function metrics
radon>=5.1.0            # Maintainability index & Halstead metrics

# --- Git History Mining ---
pydriller>=2.4          # Commit history, churn, developer count

# --- Dependency Graph ---
networkx>=3.0           # File import dependency graph construction

# --- HTTP Requests (LLM API calls use stdlib urllib, no extra dep needed) ---
cryptography>=41.0.0

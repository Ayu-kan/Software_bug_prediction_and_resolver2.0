{
  "name": "software-bug-prediction-and-resolver",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
